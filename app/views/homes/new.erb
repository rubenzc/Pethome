<% if @home.errors.any? %>
    <ul>
      <% @home.errors.full_messages.each do |error| %>
        <li> <%= "#{error}" %> </li>
      <% end %>
    </ul>
<% end %>

<h1>New Home pa ti</h1>
<%= render "form" %>
<br/>
<%= link_to("Cancel", homes_path) %>